// AUTOGENERATED FILE - DO NOT EDIT

<%- ISA.signatures_definition.each do |signature_name, signature_def| -%>
struct <%= signature_name %>
{
    void Set(const uint8_t *insts_buff)
    {
        static_assert(sizeof(<%= signature_name %>) == sizeof(uint32_t));
        std::memcpy(this, insts_buff, sizeof(uint32_t));
    }

    std::string Dump(const std::string &prefix);

    uint32_t
    <%- signature_def.each_with_index do |(field_name, bit_width), index| -%>
        <%- if (index != signature_def.size - 1) then -%>
        <%= field_name %> : <%= bit_width %>,
        <%- else -%>
        <%= field_name %> : <%= bit_width %>;
        <%- end -%>
    <%- end -%>
} <%= signature_name.downcase %>;

<%- end -%>

